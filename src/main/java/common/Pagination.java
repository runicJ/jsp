package common;

import javax.servlet.http.HttpServletRequest;

public class Pagination {
	
	public static void pageChange(HttpServletRequest request, int pag, int pageSize) {  // 메소드 만들어오기(뭐를 넣을지 정하자, 값을 받아와야함, page, pageSize) => 페이지 vo를 만들어 와야함(기존 것 백업 받고 안전하게 해야함)
		
	}
}
